<h1> <%= @user.name %>'s Profile </h1>

<ul>
    <li>Age: <%= @user.age %> </li>
    <li>Bio: <%= @user.bio %> </li>
</ul>

Reviews:
<ul>
    <% @user.reviews.each do |i| %>
        <li><%= link_to i.hike.name, review_path(i) %></li>
    <% end %>
</ul>

<%= button_to 'Create', new_review_path, method: "get" %>
